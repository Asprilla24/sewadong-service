source 'https://rubygems.org'

gem 'standalone_migrations'
gem 'pg'
# rein adds constraints to migrations
gem 'rein'

# These are usually included with Ruby by default, but the alpine apk install
# of rubygems doesn't include them.
gem 'bigdecimal'
gem 'json'
